---
import ProjectDetail from '../../components/ProjectDetail.astro';
import { loadProject } from '../../db';

const project = loadProject(Astro.params.slug!);
if (!project) return Astro.rewrite('/404');
---

<ProjectDetail locale="tr" project={project} />
